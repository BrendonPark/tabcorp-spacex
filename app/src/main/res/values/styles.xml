<resources>

    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowBackground">@color/colorWindowBackground</item>
    </style>

    <style name="RowLabel" parent="TextAppearance.AppCompat.Body2">
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_marginEnd">@dimen/label_value_margin</item>
        <item name="android:layout_marginTop">@dimen/row_margin</item>
        <item name="android:layout_width">0dp</item>
    </style>

    <style name="RowValue">
        <item name="android:layout_marginTop">@dimen/row_margin</item>
        <item name="android:layout_marginStart">@dimen/label_value_margin</item>
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textAlignment">textEnd</item>
    </style>

    <style name="CardView.Flat">
        <item name="cardElevation">0dp</item>
    </style>
</resources>
